apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventory_srv.fullname" . }}
  labels:
    {{- include "inventory_srv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inventory_srv.selectorLabels" . | nindent 4 }}
